<h1><%= @registered_application.name %></h1>
<div class='row'>
    <div class='col-md-8'>
        <h5> URL: <%=link_to @registered_application.url, @registered_application.url, :target=> '_blank' %></h5>
        <ul class= 'list-unstyled'>
            <% @events.each do |name, events| %>
                <h2><%= name %></h2>
                <li><p> Number of events: <%= events.count %></p>
            <% end %></li>
        </ul>
    </div>
    <div class='col-md-8'>
        <%= link_to "Delete Application", registered_application_path(@registered_application), method: :delete, class:'btn btn-danger', data: { confirm: "Are you sure you want to delete this application?" } %>
    </div>
</div>

